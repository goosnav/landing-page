Reserved for a future fully configured sample business variant.

This directory exists to preserve the template structure defined in the
source specification without introducing duplicate production content.
